{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug main.cpp with LLDB",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/main",
            "args": [],
            "cwd": "${workspaceFolder}",
            "stopOnEntry": false,
            "preLaunchTask": "build active file"
        }
    ]
}
